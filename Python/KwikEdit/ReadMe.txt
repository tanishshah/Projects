KwikEdit is a tool which uses OpenCV to perform image operations and tkinter to create a gui to display them.

Currently it requires a base image to be in the directory that the script is in to run as inteneded.